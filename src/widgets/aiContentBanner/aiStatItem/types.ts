export type AnalisDescription = {
  icon: string;
  title: string;
  description: string;
};

export type AIStatItemType = {
  analizLabel: string;
  percentage: string;
  weeklyChange: string;
  analizDescription: AnalisDescription;
};
